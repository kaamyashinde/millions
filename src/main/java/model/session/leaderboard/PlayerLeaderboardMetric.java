package model.session.leaderboard;


/**
 * Supported leaderboard ranking metrics.
 */
public enum PlayerLeaderboardMetric {
  /** Rank players by restored net worth. */
  NET_WORTH,
  /** Rank players by total return percentage. */
  TOTAL_RETURN_PERCENT
}
